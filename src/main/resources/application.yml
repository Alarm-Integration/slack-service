server:
  port: 30010

eureka:
  instance:
    prefer-ip-address: true
    ip-address: "${HOST_IP}"

spring:
  application:
    name: slack-service

kafka:
  bootstrap:
    servers: 10.7.27.17:9092

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans, busrefresh